// import nock from 'nock'
// import 'jest-extended'

// jest.setTimeout(10_000)

// beforeAll(() => {
//   // disable all external network requests, except for services on localhost
//   nock.disableNetConnect()
//   nock.enableNetConnect('localhost')

//   jest.clearAllMocks()
//   jest.resetAllMocks()
// })

// afterAll(() => {
//   nock.cleanAll()
//   nock.restore()

//   jest.clearAllMocks()
//   jest.resetAllMocks()
// })
